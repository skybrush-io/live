/**
 * Clock skew threshold that triggers a warning on the UI that prompts the user
 * to fix the clocks.
 */
export const CLOCK_SKEW_WARNING_THRESHOLD = 1000;

/**
 * Maximum roundtrip time in milliseconds that we do not consider a problem when
 * communicating with the server.
 */
export const MAX_ROUNDTRIP_TIME = 500;
