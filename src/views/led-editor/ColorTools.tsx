/**
 * @file Deprecated — the painting tools moved into `ColorPanel`. This file is
 * kept only as a compatibility re-export because the OneDrive-synced workspace
 * tends to resurrect deleted files; it must stay valid so it never breaks the
 * build. Do not add new code here — use `ColorPanel` instead.
 */

export { default } from './ColorPanel';
