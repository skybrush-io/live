/**
 * @file Placeholder panel for the LED-show simulation feature (TODO).
 *
 * Registered in the workbench so the feature exists end-to-end; the actual
 * 2D/3D playback simulation of the compiled show is not implemented yet.
 */

import React from 'react';

import { BackgroundHint } from '@skybrush/mui-components';

const LedSimulationPanel = (): JSX.Element => (
  <BackgroundHint text='LED show simulation — coming soon' />
);

export default LedSimulationPanel;
